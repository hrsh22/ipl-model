Task 7: Wire observer service and trading status around active scoreboard mode

Modified files:
- src/observer/service.ts
- src/trading/api.ts
- tests/observer-service-scoreboard-intents.test.ts
- tests/trading-api.test.ts

Verification:
- pnpm exec vitest run tests/observer-service-scoreboard-intents.test.ts tests/trading-api.test.ts
  Result: PASS (2 files, 9 tests)
- pnpm typecheck
  Result: PASS

Notes:
- Observer service now evaluates the existing createObserverTradeIntent bridge immediately after live-model view construction, before live-model signal thresholding and journal persistence throttles.
- The new observer service test covers a live second-innings ball-66 update with only +/-100 bps model edge and verifies the bridge is still called while no live-model signal journal row is written.
- Trading status now includes a sanitized activeStrategy summary for scoreboard-side-11-13, default value90 mode, dry-run state, live readiness, and readiness blockers.
- Trading API redaction now covers funded account identifier fields in addition to private keys, bearer/auth material, signatures, raw orders, and credentials.

Task 7 verification fix:
- Moved the live-model persistence-disabled short-circuit so buildLiveModelView(...) and evaluateObserverTradeIntent(...) still run after journal persistence has been disabled.
- Added a regression test that first disables live-model journal persistence with a schema-style snapshot write failure, then verifies a subsequent live ball update still calls createObserverTradeIntent without attempting more journal writes.

Verification after fix:
- pnpm exec vitest run tests/observer-service-scoreboard-intents.test.ts tests/trading-api.test.ts
  Result: PASS (2 files, 10 tests)
- pnpm typecheck
  Result: PASS
